export const safeMethods = ["GET", "OPTIONS", "HEAD"];
export const unsafeMethods = ["POST", "PUT", "PATCH", "DELETE"]; 

export enum Methods {
   GET,
   POST,
   PATCH,
   PUT,
   DELETE,
   OPTIONS,
}